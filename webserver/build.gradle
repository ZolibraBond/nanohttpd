description = 'NanoHttpd-Webserver'

dependencies {
    api project(':nanohttpd')

    testImplementation group: 'org.apache.httpcomponents', name: 'httpclient', version: '4.4.1'
}

task wrapper(type: Wrapper) {
    gradleVersion = "8.1.1"
}
